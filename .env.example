# ====================================================================
# REQUIRED SUPABASE VARIABLES (for Next.js and API Routes)
# See SETUP_INSTRUCTIONS.md to get these values from your Supabase Dashboard.
# ====================================================================

# Publicly accessible URL for the Supabase project
NEXT_PUBLIC_SUPABASE_URL=YOUR_SUPABASE_PROJECT_URL_HERE
# Public 'anon' key for the Supabase project
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY_HERE
# Service role key used for server-side operations (e.g., in /api/[[...path]]/route.js)
SUPABASE_SERVICE_ROLE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY_HERE

# ====================================================================
# APPLICATION CONFIGURATION
# ====================================================================

# Base URL for the deployed application (or localhost during development)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
# Allowed CORS origins for the API (use '*' for public access)
CORS_ORIGINS=*

# ====================================================================
# OPTIONAL/LEGACY MONGODB VARIABLES
# These variables appear to be present in your config but may be unused 
# if you are only using Supabase/PostgreSQL as per your setup.
# ====================================================================

MONGO_URL=mongodb://localhost:27017
DB_NAME=your_database_name